[project]
name = "resume-tailor"
version = "1.0.0"
requires-python = "~=3.12.0"

[tool.vercel.scripts]
build = "PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers PLAYWRIGHT_SKIP_BROWSER_GC=1 python -m playwright install --only-shell chromium"
